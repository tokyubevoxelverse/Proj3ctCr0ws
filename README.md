# Project Cr0ws — Prometheus

**Created Infinitive Virtual MultiVerse · Born from Prometheus 3.**
The living codebase brain of the Tokyube VoxelVerse engine (`ChunkWorldGen`, v0.0.137).
Cr0ws is the codename for Tokyube. Project Scorch remains the VR twin.

One self-contained `index.html` — React 18 + a purged Tailwind utility layer, a living neural
brain rendered on canvas, binary matrix rain, and the complete engine inventory
(every file, all 100 REST routes, all 217 codeblocks, all 27 components, cloud schema, save folders).

## Access

The entire site is **AES-256-GCM encrypted** (PBKDF2, 250k iterations). It is ciphertext until
the key is entered — view-source reveals nothing.

- Password: `cr0ws` (any casing works)
- Unlock persists for the browser session.

## Deploy to GitHub Pages

1. Push this folder's contents to `https://github.com/tokyubevoxelverse/Proj3ctCr0ws` (branch `main`, repo root).
2. Repo → **Settings → Pages** → Source: **Deploy from a branch** → Branch: `main` / `/ (root)` → Save.
3. Live at: `https://tokyubevoxelverse.github.io/Proj3ctCr0ws/`

No build step, no dependencies, works on desktop and mobile.

```powershell
cd "D:\Massive Multiplayer Online Game\Proj3ctCr0ws"
git init -b main
git add index.html .nojekyll README.md
git commit -m "Project Cr0ws — Prometheus neural archive"
git remote add origin https://github.com/tokyubevoxelverse/Proj3ctCr0ws.git
git push -u origin main
```

---
Compiled read-only from `ChunkWorldGen` on 2026-07-20 — zero files were written into the engine.
